import { HousePlan } from '../types';

/**
 * Default house plans array.
 * Empty by design so all house plans are strictly loaded from Supabase storage / database only.
 */
export const defaultHousePlans: HousePlan[] = [];
